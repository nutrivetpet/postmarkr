% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{stats_get}
\alias{stats_get}
\title{Get Statistics from Postmark}
\usage{
stats_get(client, params, ...)
}
\arguments{
\item{client}{A Postmarkr client object created with \code{\link[=Postmarkr]{Postmarkr()}}.}

\item{params}{Optional Stats parameters object created with \code{\link[=Stats]{Stats()}}.
If not provided, no query parameters will be sent.}

\item{endpoint}{character. The Stats endpoint path (e.g., "overview",
"sends", "bounces", "opens/emailclients", "clicks",
"clicks/browserfamilies"). The "/stats/" prefix and message stream
will be added automatically.}
}
\value{
A postmarkr_response object containing the statistics data.
}
\description{
Generic function to retrieve statistics from various Postmark API endpoints.
This provides a flexible way to query different types of statistics using
the same interface.
}
\examples{
\dontrun{
# Create a client
client <- Postmarkr(
  token = "your-server-token",
  message_stream = "outbound"
)

# Get sent counts with date range
params <- Stats(
  fromdate = "2024-01-01",
  todate = "2024-01-31"
)
stats_get(client, params)

# Get open counts filtered by tag
params <- Stats(
  tag = "welcome-email",
  fromdate = "2024-01-01",
  todate = "2024-01-31"
)
stats_get(client, params, "opens")

# Get click statistics by browser family
stats_get(client, params, "clicks/browserfamilies")
}

}
\seealso{
\url{https://postmarkapp.com/developer/api/Stats-api} for Postmark Stats API
documentation
}
