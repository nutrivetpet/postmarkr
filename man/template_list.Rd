% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template.R
\name{template_list}
\alias{template_list}
\title{List Postmark Templates}
\usage{
template_list(count, offset = 0L, type = "all", token = NULL)
}
\arguments{
\item{count}{An integer specifying the number of templates to retrieve.}

\item{offset}{An integer specifying the offset for pagination. Defaults to 0.}

\item{type}{A string specifying the template type to filter by: "all",
"standard", or "layout". Defaults to "all".}

\item{token}{Character string. Your Postmark API token. If NULL (default), the
function will attempt to retrieve the token using \code{get_token()}
\code{POSTMARK_SERVER_TOKEN} as an environment variable. Check the Postmark's API
documentation on how to get a server token.}
}
\value{
A data frame (or tibble if tibble is installed) containing the
templates information. The returned data includes template details from the
Postmark API.
}
\description{
Retrieves a list of templates from the Postmark API. Templates
can be filtered by type and paginated using count and offset parameters.
}
\examples{
\dontrun{
# Get the first 10 templates
templates <- template_list(count = 10)

# Get 20 templates, starting from the 11th template
more_templates <- template_list(count = 20, offset = 10)

# Get only layout templates
layouts <- template_list(count = 50, type = "layout")

# Use a specific token
templates <- template_list(count = 10, token = "your-api-token")
}
}
