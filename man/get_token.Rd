% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/token.R
\name{get_token}
\alias{get_token}
\title{Get Postmark API Token}
\usage{
get_token(env = c("prod", "test"))
}
\arguments{
\item{env}{Character string specifying the environment. Either \code{"prod"} for
production or \code{"test"} for testing. Defaults to \code{"prod"}.}
}
\value{
A character string containing the API token.
}
\description{
Retrieves the Postmark API token from environment variables for
authentication with the Postmark API.
}
\section{Environment Variables}{

The function looks for the following environment variables:
\itemize{
\item \code{POSTMARK_PROD_SERVER_TOKEN} - for prod/production environment
\item \code{POSTMARK_TEST_SERVER_TOKEN} - for test environment
}
}

\section{Test Mode}{

When using \code{env = "test"}, you can configure a sandbox server in Postmark
that processes emails without actually delivering them. This is useful for
development, testing, and CI/CD pipelines. For more information, see the
\href{https://postmarkapp.com/developer/user-guide/sandbox-mode}{Postmark Sandbox Mode documentation}.
}

\examples{
\dontrun{
# Set environment variable first
Sys.setenv(POSTMARK_SERVER_TOKEN = "your-token-here")

# Get prod token
token <- get_token("prod")

# Get test token
Sys.setenv(POSTMARK_TEST_SERVER_TOKEN = "your-test-token")
test_token <- get_token("test")
}
}
